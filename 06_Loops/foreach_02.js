const valuess = ["shouqat", "Mohammad", "Al-Azeez"]
const val = valuess.forEach(function (val) {// if we decalre the variables it will print along with the undefined value so better not to use the ***const val = valuess.forEach(function (val)***
    console.log(val);

})
console.log(val);

const values = ["shouqat", "Mohammad", "Al-Azeez"]// for each will not return the values
values.forEach(function (val) {
    console.log(val);

})

