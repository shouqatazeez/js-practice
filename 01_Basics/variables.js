let num = 10
num = 90
console.log(num);

const keyValue = 125
// keyValue = 25  // once we decalre the value in the with const then we can't modify it agian  
console.log(keyValue);

var value = 67; // perfer not to use var due to scope issues  use let only it is the best approach
console.log(value);

